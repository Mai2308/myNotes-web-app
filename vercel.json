{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd ../api && mkdir -p public && cp -r ../frontend/build/* public/",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
