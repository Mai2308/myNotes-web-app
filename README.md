# myNotes-web-app
A simple full-stack notes application built with React and Node.js.

✨ Features:
-User authentication
-Create, edit, delete notes
-Dark & light theme
-Create folders
-Sort and search notes 

🛠️ Built with:
-Frontend: React,CSS, Axios
-Backend: Node.js, Express
-Database: MongoDB Atlas
-Tools: Git, GitHub

🚀Getting Started:

  🔧 Prerequisites:
   Before running this project, make sure you have the following installed:
   - Node.js (v18+ recommended) – required to run the backend and frontend
   - npm (comes with Node.js) – used to install dependencies
   - Git – to clone and manage the repository
   - MongoDB Atlas account (or MongoDB Community Server if using a local database)
   - Postman (optional) – for testing API routes
   - VS Code or any code editor (optional)
     
1️⃣ Clone the Repository
2️⃣ Install dependencies:
    *npm install
3️⃣ Run the App
   *Frontend:
     npm start
   *Backend:
     npm start
