# Backend environment example
PORT=5000
# Local MongoDB (replace if using Atlas)
MONGO_URI=mongodb://127.0.0.1:27017/myNotesDB
# JWT secret for signing tokens
JWT_SECRET=change_this_secret
# (Optional) SQL Server variables if you still use SQL Server
SQL_SERVER=sqlserver
SQL_USER=sa
SQL_PASSWORD=your_sql_password
SQL_DATABASE=NotesDB
DB_HOST=localhost
